enum VelocityAccessibilityComponentType {
  button,
  textField,
  checkbox,
  switchComponent,
  slider,
  other,
}

enum VelocityAccessibilitySemanticType {
  label,
  hint,
  value,
  description,
}

enum VelocityAccessibilityNavigationDirection {
  next,
  previous,
  up,
  down,
}
